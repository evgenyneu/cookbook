<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Cookbook demo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class='layout-outer'>
      <div class='layout-fixed'>
        <div class='isTextCentered Head'>
          <%= link_to 'Admin', recipes_path, class: 'Head-adminLink' %>
          <%= link_to search_path, class: 'Logo' do %>
            <%= image_tag 'cookbook_logo.png', class: 'Logo-image' %>
            <div class='Logo-title'>Cookbook</div>
          <% end %>
        </div>

        <br>

        <% if flash[:error] %>
          <p id="error"><%= flash[:error] %></p>
        <% end %>

        <% if notice.present? %>
          <p id="notice"><%= notice %></p>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
